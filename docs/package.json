{
  "name": "inference-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "python3 scripts/render_pyi.py && python3 scripts/render_examples.py && cargo test -p inference-cli --manifest-path ../Cargo.toml regenerate_cli_reference -- --ignored && cargo test -p inference-server-core --manifest-path ../Cargo.toml regenerate_openapi -- --ignored && cargo test -p inference-core --manifest-path ../Cargo.toml regenerate_supported_models -- --ignored",
    "render-pyi": "python3 scripts/render_pyi.py",
    "refresh-openapi": "cargo test -p inference-server-core --manifest-path ../Cargo.toml regenerate_openapi -- --ignored",
    "predev": "npm run render-pyi",
    "prestart": "npm run render-pyi",
    "prebuild": "npm run render-pyi",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.3",
    "astro": "^7.3.4",
    "sharp": "^0.35.4",
    "starlight-openapi": "^0.26.2"
  },
  "overrides": {
    "form-data": "^4.0.6"
  }
}
